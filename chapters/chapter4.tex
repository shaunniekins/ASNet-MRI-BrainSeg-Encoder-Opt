\chapter{RESULTS AND DISCUSSION}

\section{Introduction}
This chapter presents the results from the experiments detailed in Chapter 3. Five variants of the Attention Synergy Network (AS-Net), differing only in their encoder backbone (VGG16, MobileNetV3-Large, MobileNetV3-Small, EfficientNetV2-B0, EfficientNetV2-B1), were trained and evaluated on the preprocessed BraTS 2023-GLI (Task1) dataset for binary brain tumor segmentation. The analysis compares these variants based on segmentation accuracy and computational efficiency (training and inference time).

\section{Experimental Setup Summary}
To provide context for the results, the key experimental parameters are briefly reiterated:
\begin{itemize}
  \item \textbf{Dataset:} BraTS 2023-GLI (Task 1) Training Data, preprocessed from NIfTI to H5 slices. 80/10/10 train/validation/test split applied to the slices.
  \item \textbf{Input:} T1ce and FLAIR modalities mapped to 3 channels ([T1ce, FLAIR, T1ce]), Z-score normalized.
  \item \textbf{Target:} Binary segmentation mask (Tumor vs. Background).
  \item \textbf{Input Sizes:} VGG16, MNv3-L/S \& ENv2-B0 (224x224), ENv2-B1 (240x240).
  \item \textbf{Architecture:} AS-Net decoder (SAM, CAM, Synergy) with specified encoder backbone (fine-tuned).
  \item \textbf{Loss:} Combined Loss (0.5 * WBCE(weight=100) + 0.5 * DiceLoss).
  \item \textbf{Optimizer:} Adam (LR=1e-4).
  \item \textbf{Training:} Max 30 epochs, Batch Size 32/replica for all models, Early Stopping (patience=15 on val\_loss), ReduceLROnPlateau (patience=5 on val\_loss), Best checkpoint saved based on val\_dice\_coef.
  \item \textbf{Evaluation:} Metrics (Dice, IoU, Accuracy, Precision, Recall) calculated on the test set using a 0.5 threshold. Training time recorded for efficiency comparison.
\end{itemize}

\section{Segmentation Performance Results}

\subsection{Quantitative Results}
The primary quantitative segmentation metrics, calculated on the held-out test set using the best checkpoint (selected based on validation Dice score), are summarized in \ref{tab:quantitative_results_updated}. The table also includes the epoch number at which the best validation Dice score was achieved, along with computational efficiency metrics.

\begin{table}[ht]
  \centering
  \captionsetup{skip=0pt}
  \caption{AS-Net test set performance and efficiency by encoder backbone.}
  \label{tab:quantitative_results_updated}
  \setlength{\tabcolsep}{3pt}
  \newcolumntype{C}{>{\Centering\arraybackslash}X}
  \begin{tabularx}{\textwidth}{@{} l C C C C C c c C C @{}}
    \hline\hline
    \textbf{Encoder} & \textbf{DC} & \textbf{IoU} & \textbf{Prec.} & \textbf{Rec.} & \textbf{Bin.} & \textbf{Best} & \textbf{Train} & \textbf{Infer.} \\
    \textbf{Backbone} & & & & & \textbf{Acc.} & \textbf{Epoch} & \textbf{Time} & \textbf{Time} \\
    \midrule
    VGG16       & \textbf{0.8873} & \textbf{0.7974} & 0.8334 & 0.9487 & 0.9974 & 19 & 23:39 & 5.43 \\
    MNv3‐Large  & 0.8738 & 0.7759 & 0.8186 & 0.9371 & 0.9971 & 16 & \textbf{18:12} & 2.77 \\
    MNv3‐Small  & 0.8570 & 0.7498 & 0.7912 & 0.9348 & 0.9967 & 21 & 18:25 & \textbf{2.42} \\
    ENv2‐B0     & 0.8853 & 0.7942 & 0.8327 & 0.9451 & \textbf{0.9974} & 17 & 18:56 & 3.84 \\
    ENv2-B1     & 0.8815 & 0.7881 & 0.8185 & \textbf{0.9550} & 0.9973 & 21 & 19:22 & 4.22 \\
    \bottomrule
  \end{tabularx}%
  \caption*{\textit{Note.} DC: Dice Coefficient (/F1 Score); IoU: Intersection over Union; Prec.: Precision; Rec.: Recall; Bin. Acc.: Binary Accuracy. Training Time (HH:MM); Inference Time (ms/slice). Values reflect performance on the test set using best validation checkpoint. Hardware: NVIDIA Tesla V100 GPU (32GB), Intel Xeon Silver 4210 CPU, 16GB RAM. Best values are bolded (excluding time metrics for clarity). Best time values are also bolded.}
\end{table}

\subsection{Training History}
All models were trained using the Adam optimizer with an initial $1 \times 10^{-4}$ learning rate. During training, metrics such as loss, Dice Coefficient, IoU, and accuracy were logged for both the training and validation sets at the end of each epoch, as recorded for each encoder variant. These logs generated training history plots (see \ref{app:supplementary_figures}).

The training process incorporated two key callbacks based on the validation loss (\texttt{val\_loss}):
\begin{itemize}
  \item \textbf{ReduceLROnPlateau:} If \texttt{val\_loss} did not improve for five consecutive epochs, the learning rate was reduced by 2. Analysis of the training histories shows this callback was triggered for all models, typically around epoch 9 or 10 for the first reduction (e.g., VGG16 and MNv3-L at epoch 9; MNv3-S, ENv2-B0, and ENv2-B1 at epoch 10), with subsequent reductions occurring later in training if \texttt{val\_loss} continued to plateau.
  \item \textbf{EarlyStopping:} Training was configured to stop if \texttt{val\_loss} did not improve for 15 consecutive epochs, preventing overfitting and unnecessary computation. This mechanism successfully halted training for all models before the maximum 30 epochs were reached.
\end{itemize}

The training history plots and data generally showed convergence for all models. Training loss consistently decreased, while validation loss typically decreased steadily in the initial epochs before stabilizing or slightly increasing, triggering the learning rate reductions and eventual early stopping. Correspondingly, training and validation accuracy metrics (Dice, IoU, Accuracy) generally increased before plateauing. The training process for all variants appeared stable, without significant oscillations or divergence issues noted in the loss or accuracy curves. The epoch at which the best validation Dice score was achieved varied across models (ranging from 16 for MobileNetV3-Large to 21 for MobileNetV3-Small and EfficientNetV2-B1, see \ref{tab:quantitative_results_updated}), indicating differences in the learning dynamics and convergence points despite similar overall training configurations. The complete set of training history plots for all evaluated encoder variants, illustrating these learning curves, can be found in \ref{app:supplementary_figures}.

\subsubsection{Comparative Analysis of Algorithm Behaviors}
Analyzing the training histories reveals distinct behaviors among the different encoder architectures:

\begin{itemize}
  \item \textbf{Convergence Speed:} VGG16 demonstrated the fastest initial convergence, reaching a validation Dice coefficient of 0.90 by epoch 6, compared to MobileNetV3-Small, which required until epoch 13 to achieve similar performance. This suggests VGG16's feature extraction capabilities may better suit the segmentation task without requiring extensive optimization.

  \item \textbf{Response to Learning Rate Reduction:} All models showed improved performance following learning rate reductions at epochs 9-10. However, the lightweight encoders (particularly EfficientNetV2-B0/B1) exhibited more significant gains post-reduction, with validation Dice scores improving by 0.01-0.02 in subsequent epochs, suggesting these architectures benefited more from fine-grained parameter adjustments.

  \item \textbf{Stability Patterns:} VGG16 maintained the most stable validation performance after reaching peak accuracy, with validation Dice fluctuating within ±0.005 after epoch 15. In contrast, MobileNetV3-Small showed more volatility (fluctuations of ±0.01), potentially indicating a less stable optimization landscape due to its reduced parameter count limiting representational capacity.

  \item \textbf{Training-Validation Gap:} EfficientNetV2 variants exhibited the narrowest gaps between training and validation metrics throughout training (typically <0.03 difference in Dice coefficient), suggesting better generalization properties. MobileNetV3-Small showed the widest gap (up to 0.05 difference), indicating a greater tendency toward overfitting.

  \item \textbf{Fine-Tuning Phase Effectiveness:} The benefits of the fine-tuning strategy were particularly evident in VGG16 and EfficientNetV2-B1, which continued to show validation performance improvements for 8-10 epochs after the first learning rate reduction. MobileNetV3-Small showed diminishing returns more quickly, suggesting limited capacity to leverage the fine-tuning phase despite the learning rate adjustments.
\end{itemize}

These behavioral differences highlight how encoder architecture choices significantly influence the AS-Net framework's final performance metrics, learning dynamics, and training efficiency. This has implications for encoder selection in time-constrained or resource-limited environments.

\subsection{Illustrative Segmentation Examples}
Representative segmentation examples from the test set are shown in \ref{fig:qualitative_results} to provide a visual illustration of the outputs produced by each AS-Net variant compared to the ground truth.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.6\textwidth]{figures/comparison_charts/qualitative_results.png}
  \caption[Illustrative Segmentation Results]{Illustrative segmentation results from the test set for different AS-Net encoder variants. The figure shows example outputs from all encoder variants (VGG16, MNv3-L, MNv3-S, ENv2-B0, ENv2-B1) alongside the ground truth for representative test samples.}
  \label{fig:qualitative_results}
\end{figure}

\subsection{Discussion of Segmentation Performance}
Analysis of the quantitative results (\ref{tab:quantitative_results_updated}) reveals:
\begin{itemize}
  \item \textbf{Best Performance:} AS-Net-VGG16 achieved the highest Dice (0.8873) and IoU (0.7974) scores, establishing the baseline performance.
  \item \textbf{Lightweight Competitiveness:} AS-Net-EfficientNetV2-B0 demonstrated remarkably competitive performance (Dice 0.8853), nearly matching the VGG16 baseline with a difference of only ~0.2\%. EfficientNetV2-B1 (Dice 0.8815) and MobileNetV3-Large (Dice 0.8738) also achieved strong results, significantly outperforming MobileNetV3-Small (Dice 0.8570).
  \item \textbf{Encoder Comparison:} The results suggest that while VGG16 provides a slight edge in accuracy in this setup, modern lightweight architectures like EfficientNetV2-B0 can achieve nearly equivalent performance for this task. MobileNetV3-Large offered reasonable accuracy, while MobileNetV3-Small lagged behind.
  \item \textbf{Precision/Recall Trade-offs:} VGG16 had the highest precision, while EfficientNetV2-B1 had the highest recall, indicating subtle differences in how models handled false positives versus false negatives. ENv2-B0 showed a balance similar to VGG16.
\end{itemize}

These findings address Research Question 2a (accuracy comparison) and provide insights into Research Question 3 (attention effectiveness), suggesting the AS-Net attention synergy remains beneficial across different encoders.
\section{Computational Efficiency Results}

\subsection{Quantitative Results}
The total training time and average inference time per slice for each AS-Net variant are presented in \ref{tab:quantitative_results_updated}. Visual comparisons are shown in \ref{fig:training_time_comparison} and \ref{fig:inference_time_comparison}.

\begin{figure}[ht]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
        ybar,
        width=0.9\textwidth,
        height=7cm,
        bar width=20pt,
        ylabel={Total Training Time (Hours)},
        xlabel={Encoder Backbone},
        xlabel style={yshift=-30pt},
        symbolic x coords={VGG16, MNv3-L, MNv3-S, ENv2-B0, ENv2-B1},
        xtick=data,
        nodes near coords,
        nodes near coords align={vertical},
        ymin=0,
        ymax=25,
        xticklabel style={rotate=45, anchor=east},
        legend pos=outer north east
      ]
      \addplot coordinates {
        (VGG16, 23.656)
        (MNv3-L, 18.213)
        (MNv3-S, 19.255)
        (ENv2-B0, 18.941)
        (ENv2-B1, 19.376)
      };
    \end{axis}
  \end{tikzpicture}
  \caption[Training Time Comparison]{Comparison of total training times (in hours) for AS-Net variants with different encoders on the test set. Lower is better.}
  \label{fig:training_time_comparison}
\end{figure}

\begin{figure}[ht]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
        ybar,
        width=0.9\textwidth,
        height=7cm,
        bar width=20pt,
        ylabel={Average Inference Time (ms/slice)},
        xlabel={Encoder Backbone},
        xlabel style={yshift=-30pt},
        symbolic x coords={VGG16, MNv3-L, MNv3-S, ENv2-B0, ENv2-B1},
        xtick=data,
        nodes near coords,
        nodes near coords align={vertical},
        ymin=0,
        ymax=6,
        xticklabel style={rotate=45, anchor=east},
        legend pos=outer north east
      ]
      \addplot coordinates {
        (VGG16, 5.43)
        (MNv3-L, 2.77)
        (MNv3-S, 2.42)
        (ENv2-B0, 3.84)
        (ENv2-B1, 4.22)
      };
    \end{axis}
  \end{tikzpicture}
  \caption[Inference Time Comparison]{Comparison of average inference times (in milliseconds per slice) for AS-Net variants with different encoders on the test set. Lower is better.}
  \label{fig:inference_time_comparison}
\end{figure}

\subsection{Discussion of Computational Efficiency}
The training and inference times clearly demonstrate the efficiency benefits of the lightweight encoders:
\begin{itemize}
  \item \textbf{Significant Training Speed-up:} All lightweight variants trained considerably faster than the VGG16 baseline (23h 39m). MobileNetV3-Large was the fastest (18h 12m), followed closely by EfficientNetV2-B0 (18h 56m).
  \item \textbf{Quantified Training Gains:} The time reductions ranged from ~18\% (ENv2-B1, ~4.3 hours faster) to ~23\% (MNv3-Large, ~5.4 hours faster) compared to VGG16. EfficientNetV2-B0 offered a ~20\% reduction (~4.7 hours faster).
  \item \textbf{Substantial Inference Speed-up:} All lightweight models offered significantly faster inference than VGG16 (5.43 ms/slice). MobileNetV3-Small was the fastest (2.42 ms/slice, ~55\% faster), followed by MobileNetV3-Large (2.77 ms/slice, ~49\% faster), EfficientNetV2-B0 (3.84 ms/slice, ~29\% faster), and EfficientNetV2-B1 (4.22 ms/slice, ~22\% faster). These faster inference times are particularly relevant for potential clinical application, enabling quicker analysis of patient scans \parencite{diagnostics13162670}.
  \item \textbf{Encoder Complexity Correlation:} The observed training and inference times generally align with the expected computational complexity of the encoders, with VGG16 being the most demanding and the lightweight models requiring less time, despite the smaller batch size used for the latter during training.
\end{itemize}
These results directly address Research Question 2b (efficiency comparison), confirming the substantial computational savings offered by replacing VGG16 with modern lightweight backbones.

\section{Overall Discussion and Synthesis}

\subsection{Accuracy vs. Efficiency Trade-off}
Combining the segmentation accuracy and computational efficiency results reveals the trade-offs associated with each encoder choice. \ref{fig:accuracy_efficiency_paired_bar} illustrates this relationship by plotting the Dice Coefficient and the total training time for each variant.

\begin{figure}[ht]
  \centering
  \begin{tikzpicture}
    \pgfplotsset{
      compat=1.18,
      width=0.95\textwidth,
      height=8cm,
      symbolic x coords={VGG16, MNv3-L, MNv3-S, ENv2-B0, ENv2-B1},
      xtick=data,
      xticklabel style={rotate=45, anchor=east, font=\small},
    }

    \begin{axis}[
        name=plot1,
        axis y line*=left,
        ybar=0pt,
        bar width=10pt,
        ylabel={Dice Coefficient},
        ymin=0.84, ymax=0.90,
        nodes near coords,
        nodes near coords style={font=$\scriptscriptstyle$, rotate=90, anchor=east},
        legend style={at={(0.5,-0.35)}, anchor=north, legend columns=2},
        legend cell align={left},
        legend to name=commonlegend,
        enlargelimits=true,
        enlarge x limits=0.15,
      ]

      \addplot+[ybar, fill=blue!70, bar shift=-5pt] coordinates {
        (VGG16, 0.8873)
        (MNv3-L, 0.8738)
        (MNv3-S, 0.8570)
        (ENv2-B0, 0.8853)
        (ENv2-B1, 0.8815)
      };
      \addlegendentry{Dice Coef}

      \addlegendimage{ybar,fill=red!70}
      \addlegendentry{Time (Hours)}
    \end{axis}

    \begin{axis}[
        name=plot2,
        axis y line*=right,
        axis x line=none,
        ylabel={Total Training Time (Hours)},
        ylabel style={rotate=-180, at={(1.04,0.5)}},
        ymin=0, ymax=25,
        ytick={0,5,10,15,20,25},
        nodes near coords,
        nodes near coords style={font=$\scriptscriptstyle$, anchor=west},
        bar width=10pt,
        ybar=0pt,
        legend style={empty legend},
        enlarge x limits=0.15,
      ]

      \addplot+[ybar, fill=red!70, bar shift=5pt] coordinates {
        (VGG16, 23.66)
        (MNv3-L, 18.21)
        (MNv3-S, 19.26)
        (ENv2-B0, 18.94)
        (ENv2-B1, 19.38)
      };
    \end{axis}

    \node at ($(plot1.south)+(0,-2cm)$) {\ref{commonlegend}};
  \end{tikzpicture}
  \caption[Accuracy vs. Efficiency Paired Bar Chart]{Dice Coefficient (left axis, blue) versus Total Training Time in Hours (right axis, red) for AS-Net variants.}
  \label{fig:accuracy_efficiency_paired_bar}
\end{figure}

While AS-Net-VGG16 achieved the highest Dice score, AS-Net-EfficientNetV2-B0 provided nearly identical accuracy (~0.2\% lower Dice) with a significant (~20\%) reduction in training time. Despite its efficiency, the comparable accuracy of EfficientNetV2-B0 can be attributed to its modern architecture. It utilizes computationally efficient building blocks like MBConv (incorporating depthwise separable convolutions), Fused-MBConv, and channel attention (SE blocks), as detailed in Chapter 2. These components allow it to learn rich feature representations effectively while drastically reducing the parameter count and computational load (FLOPs) compared to the simpler, heavier convolutional stacks of VGG16 \parencite{tan2021efficientnetv2, hu2018squeeze, canziani2016analysis}. The AS-Net decoder's attention mechanisms may further help refine features extracted by the efficient encoder. MobileNetV3-Large offered the fastest training, but at the cost of a more noticeable drop in accuracy compared to VGG16/ENv2-B0. MobileNetV3-Small and EfficientNetV2-B1 represented other points on this trade-off curve.

\subsection{Encoder Suitability}
Based on these findings for MRI brain tumor segmentation using AS-Net on the BraTS 2023-GLI (Task 1) dataset:
\begin{itemize}
  \item \textbf{EfficientNetV2-B0} emerges as the most promising lightweight encoder, offering the best balance between near-optimal accuracy and substantially improved computational efficiency.
  \item \textbf{VGG16} remains the choice for maximizing accuracy if computational cost is not a concern.
  \item \textbf{MobileNetV3-Large} could be considered if training time is the absolute priority and a slight accuracy decrease is acceptable.
\end{itemize}
This addresses Objective 4 by providing recommendations based on the observed trade-offs.

\subsection{Effectiveness of AS-Net on MRI}
The strong performance achieved across multiple encoder backbones (Dice > 0.85 for all except potentially MNv3-S, depending on threshold) suggests that the core AS-Net attention synergy mechanism (SAM, CAM, Synergy) is effective and adaptable for enhancing feature representation in the context of MRI brain tumor segmentation. The attention modules likely contribute positively regardless of the specific features extracted by the encoder, supporting Research Question 3.

\subsection{Comparison to State-of-the-Art (SOTA)}
While this study focused on comparing encoder backbones within the AS-Net framework, it is helpful to contextualize the achieved performance. The best Dice scores obtained (around 0.88-0.89 for VGG16 and ENv2-B0) are competitive within automated brain tumor segmentation. However, direct comparison to the absolute top performers in the BraTS 2023 challenge is complex due to differences in training data (this study used only training data, challenge winners use validation/test data with potential external data), preprocessing, model architectures (often ensembles or highly customized 3D networks), and evaluation protocols. State-of-the-art methods like nnU-Net \parencite{isensee2021nnu}, which automatically configures a U-Net pipeline, often achieve Dice scores exceeding 0.90 on BraTS benchmarks through extensive optimization and potentially 3D processing. The results here demonstrate that the AS-Net architecture, particularly when optimized with an efficient encoder like EfficientNetV2-B0, provides a strong foundation, achieving high accuracy with a specific architectural design, even if not matching the absolute peak performance of heavily tuned benchmark systems like nnU-Net.

\subsection{Implications and Significance}
The results highlight the potential for optimizing advanced attention-based models like AS-Net for clinical feasibility. Achieving near state-of-the-art accuracy with significantly reduced training time (and likely inference time) using encoders like EfficientNetV2-B0 makes such models more practical for research, development, and potential deployment in resource-aware clinical environments.

\subsection{Limitations Revisited}
It is crucial to interpret these results within the context of the study's limitations (Chapter 1, Section 6):
\begin{itemize}
  \item Results are specific to the BraTS 2023-GLI (Task 1) dataset and the binary segmentation task.
  \item The slice-based 2D approach ignores 3D context.
  \item The specific preprocessing pipeline and fixed hyperparameters might influence outcomes.
  \item Training times are relative to the specific hardware used.
  \item The data split was slice-based, not patient-based, which might overestimate generalization compared to a patient-level split.
\end{itemize}
These factors should be considered when generalizing the findings. Further validation on diverse datasets, exploration of hyperparameter tuning per encoder, and formal statistical testing would strengthen the conclusions.